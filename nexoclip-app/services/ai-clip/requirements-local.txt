-r requirements.txt

# Optional dependencies for --mode local.
yt-dlp>=2024.1.1
faster-whisper>=1.0.0
openai>=1.0.0
google-genai>=1.0.0
# Pinned below 5.0: the 5.x wheel for Python 3.14 is missing cv2.CascadeClassifier
# (no cv2.objdetect module either), which breaks the local face-tracked auto-crop.
opencv-python>=4.8.0,<5.0
# torch is only needed if you want CUDA Whisper. CPU works without it.
# torch>=2.0
